version: '3.8'

services:
  api:
    build: .
    container_name: bookmygadi_backend
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    # Note: If you add PostgreSQL or Redis, you can link them here
